# 🔍 Advanced Hash Identifier - SOC Analyst Lab

**Author:** [Leoleny14/https://www.linkedin.com/in/leonardo-christal-533793345]  
**Role:** SOC Analyst Portfolio Project  

## 📌 Project Overview
During incident response and threat hunting, Security Operations Center (SOC) Analysts frequently encounter unidentified cryptographic hashes. These hashes typically fall into two categories:
1. **Malware Artifacts:** Extracted from malicious binaries (e.g., MD5, SHA-256).
2. **Compromised Credentials:** Dumped from servers or Active Directory environments (e.g., NetNTLMv2, Kerberos AS-REP, Linux Shadow).

This Python tool automates the triage process by analyzing the structural characteristics, character length, and encoding formats of an input string to identify its hash type. It uses Regex pattern matching for complex formats and hexadecimal length evaluation for standard cryptographic functions.

## 🚀 Features
* **Interactive CLI:** Continuous loop for rapid, consecutive hash analysis without restarting the script.
* **Malware IOC Identification:** Accurately identifies MD5, SHA-1, SHA-256, and SHA-512 hashes.
* **Credential Dump Recognition:** Parses complex formatted hashes generated by tools like Responder, Impacket, or Mimikatz.
* **Input Sanitization:** Automatically cleans accidental whitespace or quotation marks copied from log files.

## 🛡️ Supported Hash Types
**Standard / Cryptographic (File Triage):**
* MD5 / NTLM
* SHA-1 / RIPEMD-160
* SHA-256 / SHA3-256
* SHA-384 / SHA3-384
* SHA-512 / SHA3-512

**Complex / Credentials (Identity Threats):**
* NetNTLMv2 (Active Directory)
* Kerberos 5 AS-REP (AS-REP Roasting)
* SHA-512 Crypt / SHA-256 Crypt (Linux `/etc/shadow`)
* MD5 Crypt (FreeBSD / Cisco Type 5)
* Cisco Type 8 & 9
* Bcrypt & Argon2
* MySQL (4.1, 5.x, 8.x)

## 💻 Usage

### Prerequisites
* Python 3.6 or higher.
* No external libraries required (uses native `re` and `sys` modules).

### Running the Tool
1. Clone the repository:
   ```bash
   git clone [https://github.com/leoleny14/SOC-Hash-Identifier.git](https://github.com/leoleny14/SOC-Hash-Identifier.git)

   ---

### 💡 Analyst Tip on Case Sensitivity
On **Linux** and **macOS** environments (which almost all SOC analysts and security sandboxes use), file names are **case-sensitive**. 

* If your file on GitHub is saved as `hash_detector.py` (all lowercase), typing `python3 hash_detector.py` will work perfectly.
* If it were saved as `Hash_Detector.py` (capitalized), Linux would throw a `No such file or directory` error if someone typed it in lowercase.

Matching `hash_detector.py` exactly in your `README.md` ensures your documentation works smoothly for anyone who clones your repo!
